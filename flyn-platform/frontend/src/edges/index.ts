/**
 * Edge Components Index
 * ----------------------
 * Exports all custom edge components and the edgeTypes configuration.
 */

import ColoredEdge from './ColoredEdge';

// Edge types configuration for React Flow
export const edgeTypes = {
  colored: ColoredEdge,
};

export { ColoredEdge };
